import React from "react";
import Layout from "../../components/Layout";
const Client = () => {
    return (
        <Layout>
            <h1>Cliente</h1>
        </Layout>
    );
};

export default Client;
